/** How long the user must hold before addendTwo activates (both gestures). */
export const POWER_HOLD_ACTIVATION_MS = 600;

/** Delay before the visual build-up indicator begins — prevents animation
 *  flicker on short taps that are just normal addendOne interactions. */
export const POWER_HOLD_INDICATOR_DELAY_MS = 300;
